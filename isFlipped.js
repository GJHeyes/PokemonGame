function isFlipped(){ //e for event
    document.getElementById(computerSelect[0].id).classList.toggle('is-not-flipped')
    document.getElementById(computerSelect[0].id).classList.toggle('is-flipped')
    flip = true
}